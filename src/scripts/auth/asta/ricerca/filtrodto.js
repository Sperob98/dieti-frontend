class FiltroDto {
    constructor(categoria, tipoAsta, nomeProdotto, pagina, elementiPerPagina) {
        this.categoria = categoria;
        this.tipoAsta = tipoAsta;
        this.nomeProdotto = nomeProdotto;
        this.pagina = pagina;
        this.elementiPerPagina = elementiPerPagina;
    }
}
